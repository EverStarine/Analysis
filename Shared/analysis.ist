% 《分析学》三类索引（中文索引、外文索引、符号索引）的 makeindex 样式。
% 条目标签与页码链接之间改用弹性空白分隔：页码链接列右对齐至行末；
% 页码列表内部以全角逗号加空格“， ”分隔，页码区间以 "--" 连接。
% 用法：在索引输出目录分别执行三次命令；路径加引号以兼容空格。
%   makeindex -s "<本文件绝对路径>" chinese.idx
%   makeindex -s "<本文件绝对路径>" foreign.idx
%   makeindex -s "<本文件绝对路径>" symbols.idx
% 不得把多个 .idx 交给同一次调用，以免按合并语义写入首个 .ind。
% 正文页码变动后必须重新运行索引处理器，不得沿用旧 .ind 判断成品已更新。
delim_0 "\\nobreak\\hfill "
delim_1 "\\nobreak\\hfill "
delim_2 "\\nobreak\\hfill "
delim_n "， "
