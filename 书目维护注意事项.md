# 《分析学》书目数据库维护注意事项

版本：0.1（2026-09-03）

本文件规定三卷共用书目数据库 `Shared/References.bib` 的字段级维护准则。数据库面向 `biblatex + biber`；文献资格与采集流程见 `写作规范.md` 第 7 节与 `工作流程说明.md` 第 8 节。

维护 `References.bib` 时，不应仅以“能够被 LaTeX/Biber 编译”为正确标准，而应同时核实**文献真实性、版本一致性和 BibLaTeX 数据模型合法性**。新增或修改条目时，应优先依据出版社、期刊、DOI、作者或机构官方网站核验，并特别注意以下问题：

1. **不得混用不同版本的元数据。** `year`、`edition`、`publisher`、`series`、`volume` 和 `isbn` 应尽可能对应同一实体版本。尤其应避免把原版年份与多年后电子化产生的 eBook ISBN 混在同一条记录中。后期生成的 DOI 可以作为稳定标识保留，但不应据此误判出版年份或版次。

2. **区分作品年份、版次年份与重印年份。** 对经典著作的新版、修订版或重印本，应明确记录实际引用版本；必要时使用 `origdate` 表示原版或该版首次出版时间，而以 `year` 表示实际引用的出版/重印版本。

3. **正确选择 entry type。**

   - 专著用 `@book`；
   - 期刊论文用 `@article`；
   - 同一作者专著中的章节可用 `@inbook`；
   - 编辑文集中的独立文章用 `@incollection`；
   - 持续更新的网站、在线讲义等优先使用 `@online`，并记录 `urldate`；
   - 多卷著作必要时使用 `@mvbook` 或拆分为各卷条目。

   不应为了方便把所有非正式资料都塞进 `@misc`。

4. **字段必须与 entry type 相容。** 例如普通 `@article` 不应加入 `publisher`；应使用 `journaltitle`、`volume`、`number`、`pages`、`doi` 等相应字段。修改后应运行
   `biber --tool --validate_datamodel`
   检查非法或缺失字段。

5. **`note` 只保存真正的书目信息。** 诸如“仅供背景阅读”“不进入正式参考文献”“元数据已人工核验”“ISBN 待核”等内部编辑说明，不应放入 `note`，因为 bibliography style 可能直接打印该字段。内部说明应改放 `annotation`，状态控制则宜使用 `keywords`，例如 `status:background`。

6. **正确处理机构作者。** 机构名称不能按普通人名解析。例如 NIST 一类 corporate author 应采用双重大括号保护：
   `author = {{National Institute of Standards and Technology}}`
   或根据条目性质使用 `organization`，否则 Biber 可能把机构名称错误拆成多个作者。

7. **避免 DOI 与 DOI-URL 重复。** 已存在
   `doi = {10....}`
   时，一般不再保留
   `url = {https://doi.org/10....}`。
   `url` 应主要用于出版社页面、作者主页、开放讲义或其他 DOI 之外具有独立价值的资源。

8. **在线资源应记录访问日期。** 对 DLMF、在线课程讲义、作者主页上的动态文档等，应使用 `urldate`。对于有固定 DOI 的正式论文和专著，一般无需记录 DOI resolver 的访问日期。

9. **版次、丛书和卷号应结构化记录。** 不应把 `Second Edition`、`PMS-30`、`GSM 181` 等信息随意塞入 `title`；应分别使用 `edition`、`series`、`volume` 等字段，以保证不同 bibliography style 能正确格式化。

10. **ISBN 必须核实其具体载体。** 同一本书可能同时具有 hardcover、paperback、eBook 等多个 ISBN。不能因为某 ISBN 出现在 Crossref 或出版社数据库中就默认它属于正文所记录的年份和版本。若只能确认电子 ISBN，应明确其性质，或宁可暂不记录，也不要录入错误 ISBN。

11. **作者、编者和贡献者必须区分。** 不应把 series editor、volume editor、修订者、协作者等全部写入 `author`。应根据官方 title page 或出版社记录分别使用 `author`、`editor` 以及必要的 `note`。

12. **内部管理字段应保持机器可读。** `keywords = {ledger:b?:ref:??}` 等字段虽然默认不打印，但对三卷书共享数据库的追踪和筛选有重要作用，应保留。需要控制某条文献是否进入正式 bibliography 时，优先增加例如 `status:background`、`status:exception-candidate` 等 keyword，而不要依赖自然语言备注。

13. **特殊字符必须符合 TeX 语法。** 例如标题中的 `&` 必须写成 `\&`；同时应注意专有名词、缩写及数学符号所需的大小写保护，例如 `{Fourier}`、`{Sobolev}`、`{PDE}` 等，防止 bibliography style 自动改变大小写。

14. **修改 citation key 应极其谨慎。** 即使原 key 中年份与最终核实年份不完全一致，只要已经被正文引用，原则上仍应保持 key 不变，除非同时对全文引用进行可靠的全局替换。citation key 是内部标识，不等同于书目元数据。

15. **每次批量修改后进行两层验证。** 第一层是 Biber 数据模型验证，确保没有 warning/error；第二层应建立最小测试文档，以 `\nocite{*}` 实际排出全部条目，检查作者解析、中文、特殊字符、edition、DOI、URL 和 note 等最终显示效果。

总原则是：**Bib 文件应被视为结构化书目数据库，而不是能够勉强编译的文本文件。真实性核验、版本一致性、字段语义正确和最终排版验证四者缺一不可。**
