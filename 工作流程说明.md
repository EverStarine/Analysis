# 《分析学》项目工作流程

版本：0.1（2026-09-02）

本文件记录项目的基本操作流程和容易遗漏的检查点。当前只建立框架；以后遇到可复现的问题，再把根因、修复办法和验证方式补充进来。

## 1. 开始前检查

1. 确认本次工作的目标卷、编、章和节，不顺手扩大修改范围。
2. 阅读 `计划纲要.md` 及与任务有关的格式、术语、写作和版权规范。
3. 查看 `git status`，识别并保护已有修改；不覆盖来源不明的用户文件。
4. 结构性或排版修改前，先检查主文件、聚合文件和相关输入链。必要时保留最近一次可编译结果作为比较基线。

## 2. 按层级编辑

项目的基本依赖顺序为：

`BookN.tex → FrontMatter / PartNN / Appendices / BackMatter → ChapterNN → SectionNNNN`

- `BookN.tex` 只负责全卷设置和各大部分的聚合。
- `PartNN.tex` 负责编标题并载入章。
- `ChapterNN.tex` 负责章标题、章级内容和节文件载入。
- 正文以 `SectionNNNN.tex` 为最小文件层级，不再继续拆分目录。
- 共用排版和语义宏集中放在 `Shared/`，避免三卷分别修补后发生漂移。
- 只有层级发生变化时才修改上层聚合文件；普通正文修改尽量限制在对应节内。

## 3. 编译流程

1. Book1、Book2、Book3 始终作为三个独立项目编译。
2. 从相应卷目录启动 XeLaTeX，把辅助文件输出到 `tmp/build/BookN/`，不要直接写入源码目录或项目根目录。
3. 先解决首个致命错误，再处理其后的警告，避免被连锁报错干扰判断。
4. 修改共享样式或全局宏后，必须重新编译三卷；只改某一卷正文时，至少完整编译该卷。
5. 正式验收通常运行两轮 XeLaTeX，使目录、引用和书签稳定；接入书目或索引工具后，再补充相应的完整工具链。
6. 验收通过后，只把 `Book1.pdf`、`Book2.pdf`、`Book3.pdf` 更新到项目根目录。

## 4. 结构与 PDF 验收

每次结构性修改至少检查以下项目：

- 输入文件全部存在，magic root 指向本卷主文件。
- 目录层级为“编—章—节”，没有缺项、重复项或错误缩进。
- PDF 书签中的章、节隶属于正确的编。
- 书头、序言、附录、后记、参考文献和三类索引的层级正确。
- 编译日志没有致命错误、未定义引用或明显的页面溢出。
- 抽查标题页、目录、编首页、章首页、附录首页和书后页面，确认没有裁切、重叠或乱码。

## 5. 附录的固定规则

附录不能继续挂在最后一个正文编下面。每卷在正文结束后统一建立一个无编号的“附录”编：

```tex
\appendix
\bookmarksetup{startatroot}
\part*{附录}
\addcontentsline{toc}{part}{附录}
```

- 每卷附录均从 Appendix A 开始，附录文件夹和主文件采用 `AppendixA/AppendixA.tex` 等命名。
- 单个附录与章同级，使用 `\chapter`，不要另造一套附录层级。
- 目录和 PDF 书签中必须先出现根层级“附录”，其下才是附录 A、B、C……。
- 进入 `\backmatter` 后再次使用 `\bookmarksetup{startatroot}`，保证后记、参考文献和索引不被收进“附录”编。
- 验收时同时检查目录和实际 PDF 书签，不能只看源码或只看编译是否成功。

## 6. `tmp/` 的保留与清理

`tmp/` 用来保存编译辅助文件、日志、页面截图和阶段性比较结果。它不需要在每次编译后清空：当前正在定位问题时，保留这些文件有助于复现和比较。

可以考虑清理较早的临时文件，当且仅当同时满足以下条件：

- 写作进度已经越过若干章或一编，或者该临时结果已经隔了几天；
- 当前源码能够重新独立编译，根目录成品 PDF 已更新；
- 旧日志、截图和比较结果不再用于当前问题；
- 删除后不会影响编译、校对或尚未完成的回归检查。

里程碑提交、对外同步或磁盘占用明显增大时，可以集中清理一次。清理前应核对目标的绝对路径和内容，只删除确认属于项目的 `tmp/` 子目录；不对工作区根目录或来源不明的目录执行递归删除。

## 7. Git 提交与同步

1. 检查 `git status`、实际差异和待提交文件，排除 `tmp/`、参考原文件及 LaTeX 辅助文件。
2. 完成与修改范围相称的编译和 PDF 验收，再更新根目录成品。
3. 一个提交尽量对应一个清晰的逻辑单元，提交说明应指出修改的卷、章节或结构性质。
4. 推送后核对本地与远端提交哈希，并确认工作区干净。
5. 不把无关修改、来源不明的文件或他人的未完成工作混入当前提交。

## 8. 问题记录模板

以后出现需要长期记住的问题，可在本文件末尾增加记录：

- 日期：
- 状态：待处理 / 处理中 / 已解决 / 暂缓
- 类型：内容 / 结构 / 编译 / 引用 / 书签 / 版式 / 索引
- 影响范围：
- 首个有效错误或现象：
- 根因：
- 处理方案：
- 修改文件：
- 验证方式与结果：
- 后续注意事项：

## 9. 完成标准

一次操作只有在源码修改、受影响卷编译、目录与书签检查、根目录 PDF 更新、临时文件处置和 Git 状态核对都完成后，才算结束。若某一步暂时跳过，应在交接说明或问题记录中明确写出。
