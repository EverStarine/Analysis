# 《分析学》项目工作流程

版本：2.1（2026-09-05）

本文件记录每轮工作的基本操作顺序和容易遗漏的检查点。具体排版、术语和写作规则分别以 `格式规范.md`、`术语规范.md`、`写作规范.md` 为准；本文件以后只追加可复现的流程问题和验收办法。

## 1. 每日开始与任务定界

1. 每天早上的第一次工作才考虑从 GitHub 拉取；先查看 `git status`，有未提交修改时不得直接覆盖。
2. 同一天后续对话不反复拉取，也不把网络同步当作每轮工作的固定动作。
3. 确认本次目标卷、编、章和节，阅读 `计划纲要.md` 及相关规范，不顺手扩大内容范围。
4. 结构或排版修改前，检查主文件、聚合文件和输入链；必要时保留最近一次可编译结果作为比较基线。

## 2. 编辑层级与单一真源

项目依赖顺序为：

`BookN.tex → FrontMatter / PartNN / Appendices / BackMatter → ChapterNN → SectionNNNN.tex`

- `BookN.tex` 只装配本卷和设置卷级状态。
- `PartNN.tex` 声明本编、标签并载入各章。
- `ChapterNN.tex` 负责章标题、章级导语和各节输入。
- `SectionNNNN.tex` 直接放在所属 `ChapterNN/` 目录中，是正文的最小文件层级；不再为单个节文件另设同名目录。
- 共用排版、定理、术语和引用宏集中在 `Shared/`，三卷不得各自复制一套。
- 修改计划中的章、节标题后，同步更新对应 `.tex` 标题；修改排版规则后，同步核查公共宏。

《计划纲要》节下的条目默认落实为该节的小节骨架：条目文本即小节标题，标题后紧跟 `\label`；以 `$$` 起止的展示公式块不构成小节；条目词尾的 `*` 表示选读小节，须用共享宏 `\OptionalSubsection`（星号随编号，标题不含星号）。条目落实与重生成按上述规则脚本化执行（一次性生成工具不入库，按规则随时可重写），生成后整卷编译，并核对目录小节数与小节标签唯一性。

新建节文件时直接生成 `ChapterNN/SectionNNNN.tex`，章聚合文件使用 `\input{PartNN/ChapterNN/SectionNNNN}`。结构脚本不得恢复 `SectionNNNN/SectionNNNN.tex` 的单文件目录。批量迁移路径后，应同时核对独立编译根指令、全部 `\input`、非 TeX 文档中的路径记录以及三个分卷的完整编译。

术语统一优先修改实际 `.tex` 中的标题命令（`\BookPart`、`\chapter`、`\section`、`\subsection`、`\OptionalSubsection`）：普通概念与类别后缀按 `术语规范.md` 中文化；人名、专名、标准缩写和数学记号保留。此类任务不改动标题以外的正文或标签。计划纲要中的原文短语可作为消歧用的暂定工作条目；除非另行核定或明确要求，不因标题翻译自动回写纲要。

## 3. 初次撰写

常见异名按术语规范 1.1 在首次引入处或脚注中说明具体教材出处，并收入索引；先核对概念和定义范围，再判断能否作为异名，不改变既有标签。后文保持主称，不把每处出现都扩写成译名对照表。

具名结果按写作规范 1.2、格式规范 1.6 直接在定理类环境标题中标名。章末自检从正文中的名称反查对应环境，避免名称只出现在小节或连接段；检查已标名结果的不同版本是否清楚，不为无名结果添加未经核定的人名。参考文献的访问日期只留在数据库内，成品输出按书目维护注意事项 0.2 统一隐藏。

首次编写一章时不自行进行正式内容审核，正式审稿由编写者人工启动。初稿完成后只做必要的编译、输入链、标签、明显笔误和结构自检，避免在尚未收到意见时反复自我改写。

开始写正文前，先从 `参考文献原文件/来源记录.md` 和 `Shared/References.bib` 选出本章的资料组：通常包括一至两部主线教材、一部用于核对边界情形或一般版本的资料，以及必要的中文术语或习题参考。能够阅读全文的资料应读到与本章有关的部分；只能核验目录或出版信息的书，不得被写成某一具体论证的直接来源。把正式文献、背景材料和例外候选分开记录。

核心中文术语须实际核对国内教材的通行主称，不能只保留英文来源后自行翻译。核定结果写入 `术语规范.md` 的对应记录，并检查当前标题、正文及索引排序键；已明确核定的译名可同步纲要，尚未核定的原文占位仍保留。本次 `signed measure` 统一为“符号测度”的依据见术语规范 1.1，不改变既有标签或数学定义。

随后列出本章的“证明义务”：中心构造的起点与终点、每个主定理所需的前置结果、最容易被一句话略过的技术桥、需要说明的有限或无穷边界，以及相应的正例、反例和习题。对采用特定版本、证明路线、反例或习题改编的地方，同时记下 citation key、所据章节或页码以及改写方式。纲要中的一个小节标题只是内容坐标，不意味着每个小节都应写成同样长度的微型摘要。

“不自行进行正式内容审核”只限制审稿轮次，不降低初稿完整性。初稿自检至少包括：

1. 顺着引用关系确认所有概念先定义后使用，前向预告没有承担当前证明；
2. 对中心构造逐步核对良定义、封闭性、可数过程、唯一性和边界情形；
3. 搜索 `proofsketch`、“同理”“逐项验证”“适当”“标准方法”等省略信号，确认它们没有隐藏实质步骤；
4. 检查每条主线是否有可计算例子、失败边界或反例，并有相应的分层习题；
5. 核对章首伴读建议、正文中的针对性引文和习题内部来源记录；确认每个 citation key 存在，推荐语说明了资料用途，背景材料没有承担正式论证；
6. 在本轮任务记录中保留成品页数、中文字符量、完整证明与证明概要数、习题数作为诊断指标，不把这些统计写进审核进度总表。指标异常时回到证明义务表检查，不以机械扩写补足数字。

每章写作状态及时登记到 `审核进度.md`。总表按卷分组，只保留“章节、状态、轮数、评价、闭环、最近审核”六列，章号链接到源码；不另列稳定标签、完整文件路径、字数或证明数统计和长篇备注。正文完成但尚未收到审稿意见时，状态简写为“待审核”；已开始正式审稿但尚未定稿时写“审核中”，轮数另列。

撰写时若某个核心定理或其假设的精确表述尚待高阶模型复核，仍可先完成上下文和证明路线，但须将该断言及待核问题置于共享环境 `coreverification` 中。该环境在 PDF 中统一显示为“待 Sol Ultra 复核”的黄色高亮框；它不是定理环境，不能代替正式编号、引用或审核记录。复核通过后改回相应的正式环境并删除高亮；被标记的章节不得标为定稿。

## 4. 处理审稿意见

1. 收到意见后逐条判断其数学正确性、必要性和与本项目规范的一致性，不能因为“审稿意见这样说”就直接照改。
2. 对正确且明确的意见实施修改；对事实可疑的意见给出核查依据；对违反现行写作或排版规范的建议列出理由，与编写者商议。
3. 每轮逐章细查都记录日期、所据版本、整体评价、意见处置和复核结果；详细记录按章折叠为文字条目，不使用包含长段落的宽表格。定稿日期记在该章最后一轮记录中。
4. 整体评价使用：退稿 / 大修 / 小修 / 接受。若为“小修后接受”，小修闭环后可按接受处理。
5. 每章至少经历两轮逐章审核才可定稿。最新评价须为接受，或小修后接受且修改已闭环。
6. 全书术语、引用、排版等横向检查单独折叠登记，不计入逐章审核轮数。历史记录保留当时结果，当前章节状态以进度总表为准。

## 5. 编译顺序

1. 三卷始终分别从 `Book1.tex`、`Book2.tex`、`Book3.tex` 编译。
2. 辅助文件输出到 `tmp/build/BookN/`，源码目录和根目录只保留源码与最终 PDF。
3. 出错时首先以 LaTeX 编译器报告的第一个有效错误为准，先解决致命错误，再处理连锁警告。
4. 修改公共样式、编号或宏后重编三卷；只改某卷正文时至少完整编译该卷。
5. 目录、引用和书签通常需要至少两轮 XeLaTeX。正文含书目和索引条目时，完整顺序为“首轮 XeLaTeX 生成 `.bcf` 与 `.idx` → 运行 Biber 生成本卷书目 → 分别运行 `makeindex` 生成三类 `.ind` → 再运行两轮 XeLaTeX”；只重复 XeLaTeX 不会生成书目或索引。每次 `makeindex` 只处理一个 `.idx`，并以 `-s` 传入 `Shared/analysis.ist` 的绝对路径；可复现的 PowerShell 命令见 `README.md`，样式要求见 `格式规范.md` 第 10 节。封面入口和目录必须各有一个顶级 PDF 书签；封面入口使用卷级约定的辨识性名称，不机械写成“封面”。书签层级由章节命令自然生成，不为已有结构节点重复手写书签；习题节点统一由 `\BookExercises` 处理，按 `格式规范.md` 第 5 节核对每章唯一性。
6. 在 PowerShell 中将 `--output-directory=../tmp/build/BookN` 作为一个完整参数传给 XeLaTeX，避免反斜杠相对路径被误识别为输入文件。
7. 验收通过后更新根目录的 `Book1.pdf`、`Book2.pdf`、`Book3.pdf`；仅作源码验证时，构建结果留在 `tmp/build/BookN/`，不覆盖既有成品。
8. 编写者要求实时验收时，在若干节形成完整内容、重要修补落盘和章末收尾等阶段及时编译目标卷，不等到整章提交才第一次构建。成功生成且无致命错误的阶段 PDF 及时复制到项目根目录的 `BookN.pdf`，供编写者直接查看；辅助文件仍在 `tmp/build/BookN/`，失败时保留上一份可读 PDF。阶段预览不等于章节定稿，上传前仍完成书目、索引、引用稳定性与必要结构检查。三类索引须逐项确认 `.ilg` 中 `0 rejected`、`0 warnings`；索引语法及范数记号的防错写法见 `格式规范.md` 第 10 节。多编译不等于多做视觉验收。

## 6. 检查层次与视觉功能的节制

### 6.1 连续写作期以正文推进为先（2026-09-05）

按编写者最新要求，降低检查核验工作的权重，优先连续撰写后续章节。这不降低陈述和证明的完整性，但不把初稿阶段变成反复审稿或排版验收。

- 每章集中做一次必要源码检查与完整构建；及时更新根目录 PDF，完成一章即按授权上传，再进入下一章，不以等待正式审核为由停写。
- 核对以编译错误、未定义引用／引文、重复标签、索引拒收和明显逻辑断点为限。只复查实际修改或出现错误的部分。
- 未改共享排版和书签结构时，不逐章重测全卷页眉、目录配色、全部索引链接或字形坐标；已有通过结果沿用，细致核验留待正式审稿或专项格式修复。
- 不为普通纯文字章节另开视觉验收或全章语言重加工。PDF 技能的常规渲染步骤在本连续写作任务中不执行；发现实质版面问题时才局部处理。
- 优先使用已核定的书目、术语与现成构建工具；不因每章增加了引文而重测整个未变动的书目库，也不花长时间修写本章并不需要的验收脚本。
- 核心结论有真实疑点时保留上下文并按既有规则统一高亮，登记待后续复核；不以降低核验比重为理由隐藏疑点。耗时的全面复核不抢占连续写作的主线。

以下详细检查层次用于出现对应问题或启动专项核验时，不作为每章必须全部重跑的清单。

检查顺序为：

1. 编译器错误与日志；
2. 输入链、计数器、目录、书签、引用和 PDF 文本等结构化检查；
3. 必要页面的局部图像渲染。

书签或书前结构变动后，使用 PDF 解析工具核对书签树：顶级应依次包含卷级约定的封面入口标题、“序言”“目录”及正文、附录、书后各节点；封面书签必须落在书名页，目录书签必须落在目录首页。此时还应局部渲染封面和目录页，确认书签改动没有影响纸面版式。

图、表、算法、交换图、复杂分页或日志无法判断的版面问题，应做局部渲染检查。普通纯文字修改不必每次使用识图；全书大规模视觉检查主要留在最终定稿前，以节省时间和计算资源。

交叉引用空格修复按 `格式规范.md` 第 7 节核对：先检查宏前后及链接显示参数内的同行空白、`~` 和源码换行边界，再在本轮生成的 PDF 中查找相应文本。若抽取文本与源码判断不一致，读取相邻字形和链接区域的坐标确认，避免把抽取器生成的空格误判为排版错误。目录配色、页眉间距和索引右对齐的回归也优先用颜色、字形位置与链接坐标核验；已能由这些证据判定时不追加页面渲染。

## 7. 附录固定检查

附录不得挂在最后一个正文编下面。每卷统一建立一个无编号“附录”编，其下是章级附录。显示编号固定为第一卷 A1、B1……，第二卷 A2、B2……，第三卷 A3、B3……；附录天然选读，不加 `*`。

每次修改附录结构后同时核对：

- 纸面目录中只有一个“附录”编；
- PDF 书签中“附录”为顶级，其子项只有本卷各附录；
- “附录”目录项的目标独立于最后一节；结合 `.toc` 中的目标名与 PDF 实际跳转页核对，不能只确认标题显示正确；
- 后记、参考文献和三类索引重新回到顶级；
- 附录定理编号在无节时为 `A1.n`，有节时为 `A1.sec.n`。

## 8. 文献采集与写作接入

正式检索前先阅读 `计划纲要.md`，按卷、编、章确定需要的经典教材和基础文献。资格门槛见 `写作规范.md`。

对每项候选资料依次检查：

1. 作者、版本、出版或正式发表信息；区分正式发表、作者自存档与纯预印本；
2. 距今时间及是否符合例外条件；
3. 来源权威性和内容准确性；
4. 是否能合法下载、在线阅读全文或至少核验所需部分；
5. 版权条件及允许的使用方式；
6. 是否属于纯预印本或未正式发表的讲义：是者默认不采用，只作为写作线索或候选背景；拟按《写作规范》第 7 节例外引用的，须满足“领域广泛引用、公认为标准、无正式发表替代”，把评估依据写进 `参考文献原文件/来源记录.md`，并经编写者核准。

可以合法保存的原文件放入本地 `参考文献原文件/`；不能下载时，把网址、访问日期、可读范围、采用与否及理由写入 `参考文献原文件/来源记录.md`。若既不能查看内容又无法可靠核验，则不作为本项目参考文献。书目元数据最终统一录入 `Shared/References.bib`。

术语核验所用正式教材也可以作为读者衔接文献收录，但须说明所读范围；仅见目录时只引用名称，不把目录当作定义或证明的证据。因编写者明确许可而突破年限门槛的条目，在来源记录及书目内部字段登记本次许可，不把局部例外扩展为取消全部文献门槛。

资料入库不等于已经用于写作。逐章工作还要完成以下闭环：

1. 章前建立“内容或证明义务—主来源—复核来源—citation key—习题来源”的简表；
2. 写作中把少量参考书说明放在序言、编导读、章首导言或定理簇之间的连接段，推荐时说明用途，不在定理陈述和证明内部硬插书单；
3. 对历史归属、特定版本、特殊证明或未展开的结果作针对性引用；完整自证的标准结果不逐条堆引文；
4. 章后搜索引文命令，核对 citation key、书目资格与语义位置，并确认书后只排出本卷实际提及的正式文献；
5. 运行 Biber 和完整 XeLaTeX、索引工具链，检查正文引文链接、参考文献页、目录与 PDF 书签。

## 9. 跨卷引用

新增跨卷引用时立即更新 `跨卷引用表.md`，登记引用处与被引用处的文件、内容概要、稳定标签、当前显示编号和引用方向。编号或结构改变后，以标签和内容概要重新核对，不依赖旧编号猜测目标。

前书引用后书原则上避免；若只是预告，正文和台账都明确标记。发现跨卷引用未登记时，本轮不能把相关章节标为定稿。

## 10. `tmp/` 的保留与清理

`tmp/` 保存构建辅助文件、日志、局部截图和阶段性比较结果。正在定位问题时保留当前基线，不在每次编译后清空。

同时满足以下条件时，可把较旧临时文件列入清理：

- 写作已越过若干章或一编，或者临时结果已隔数日；
- 当前源码能够独立重编，根目录成品已经更新；
- 旧日志、截图和比较结果不再服务于当前问题；
- 删除不会影响编译、审稿或回归比较。

在里程碑、对外同步或磁盘占用明显增加时集中清理。删除前核对绝对路径和内容，只处理已确认属于本项目的 `tmp/` 子目录。

## 11. Git 与 GitHub 节奏

- 本地提交按清晰逻辑单元进行，提交前核对差异并排除 `tmp/`、参考原文件和 LaTeX 辅助文件。
- 不在每轮对话结束时机械推送。
- 每天晚上由编写者明确要求时统一推送；若当天已经工作很多轮、达到重要里程碑或存在需要及时备份的风险，可说明理由后提前推送。
- 推送后核对本地与远端提交哈希。未到同步节点不妨碍一次正常编辑任务完成。
- 编写者明确要求“每章完成后上传 GitHub”时，以“单章正文、相关台账和该卷验收后的成品 PDF”为一个提交单元：先完整编译，再检查仅暂存该单元文件，推送后核对本地 `HEAD` 与 `origin/main` 一致。不得借此顺带提交其他卷或临时文件。

### 11.1 已授权上传的目标核对与故障分层

本项目当前既有远程为 `origin = https://github.com/EverStarine/Analysis.git`，工作分支 `main` 跟踪 `origin/main`。编写者已在本任务中反复要求按章上传，并于 2026-09-05 要求避免重复确认。后续处于这项已授权写作与上传流程内时，核对目标、文件范围和普通快进推送条件后执行，不再把同一个上传请求问一遍。本文是流程与证据记录，不替代平台权限，也不授权改换仓库、强制推送、删除远端分支或上传无关资料。

遇到上传失败，先按首个有效错误确定层次：

1. 若工具在创建进程前返回 `CreateProcess` 与安全审核拒绝，说明 Git 尚未执行；不能据此诊断为 GitHub 认证失败、网络故障或分支冲突。
2. 先做只读核对：`git remote -v`、`git branch -vv`、`git reflog show origin/main`，必要时用 `git ls-remote origin refs/heads/main` 核对真实远端。目标归属有疑问时，可以列出 Git Credential Manager 保存的 GitHub 账号名称，并核对仓库公开元数据；不得读取或输出令牌。若沙箱不能访问 Windows 凭据存储，申请最小范围的只读检查，不据此重置凭据或重新登录。
3. 向审批机制说明已有用户上传指令、具体仓库与分支、待推送提交和实际核对证据。有实质性新证据时才重新申请；审核明确拒绝后，不换工具、包装脚本或修改安全设置来绕过。能自行核实的问题先核实，确需新增权限、变更目标或处理不可自动解决的拒绝时才请编写者介入，不承诺平台永不要求确认。
4. Git 真正执行后，再按其返回区分网络、认证、非快进冲突等故障；不使用强制推送消除冲突。成功后以真实远端哈希与本地 `HEAD` 一致为准，不能把本地提交成功写成上传成功。

2026-09-05 第九章上传曾返回 `TLS connect error ... unexpected eof while reading`，同一普通推送重试后成功，实际远端哈希也一致。这类短暂连接中断先做有限重试，不误判为用户授权不足，不关闭证书验证、不重置凭据，也不为同一已授权上传重复询问。

2026-09-05 的诊断证据：上一轮推送被本地自动审核以目标归属和授权范围未确认拒绝，Git 未启动；随后只读检查确认已保存账号为 `EverStarine`，仓库为公开的 `EverStarine/Analysis`、默认分支为 `main`，`origin/main` 有多次 `update by push` 记录，真实远端仍为 `f0140aa8f4def46115717bfa63b5fa31fe814859`。此前过早要求编写者重复确认的问题，按本节流程纠正。审批机制说明参见 [OpenAI 官方 Auto-review 文档](https://learn.chatgpt.com/docs/sandboxing/auto-review)。

## 12. 问题记录模板

以后遇到需要长期记住的问题，在本文件末尾追加：

- 日期：
- 状态：待处理 / 处理中 / 已解决 / 暂缓
- 类型：内容 / 结构 / 编译 / 引用 / 书签 / 版式 / 索引 / 审稿
- 影响范围：
- 首个有效错误或现象：
- 根因：
- 处理方案：
- 修改文件：
- 验证方式与结果：
- 后续注意事项：

## 13. 单轮完成标准

一次操作在源码修改、必要编译、结构检查、相关台账更新和工作区差异核对完成后即可结束。视觉全检、远端推送和 `tmp/` 清理只在各自触发条件成立时执行，不再作为每轮必做项。

## 14. 本次问题与经验记录

### 14.0 定义语序与数学辨识度（2026-09-05）

- 定义写作先明确对象与全部条件，再命名，避免照搬英文“is called ... if ...”的语序。检查须覆盖跨行、公式及列表边界，不限于搜索同一行中的“称为”和“若”。本次只调整定义语序，不扩展为反复全章润色或数学审稿。
- 术语原文统一半角括号，在共享 `\term` 宏一次修复，保留加粗主称、星号隐藏和双语索引行为。公式撤掉自选数学字体，恢复 `unicode-math` 默认字体；不改正文西文字体。
- 同类数学括号的嵌套层级须有大小区别，不能只看源码用了 `\left` 就判定可辨；两个绝对值／范数因子的乘积显式加 `\cdot`。此类修改保持原运算、上下标及区间开闭性，不进行无关代数改写。
- 本次共享宏和数学字体影响三卷，因此集中重编三卷并更新根目录 PDF；只处理实际编译问题，核对默认字体与原文括号的输出，不另做全卷视觉验收。第十章完成后按最新指令暂停新章节写作。

### 14.1 前三章正文提要化

- 日期：2026-09-04
- 状态：已解决
- 类型：内容 / 审稿
- 影响范围：第一卷第 1--3 章及后续基础章的初稿标准
- 首个有效错误或现象：章节虽然覆盖了纲要标题，但定义、定理和证明被压成等长的微型摘要；若干技术桥没有展开，例子和边界情形不足，部分习题直接复述正文。重写前第 1--3 章约为 10、10、6 个成品页，中文字符量约为 3990、3806、2241，后两章的证明概要分别有 4、7 处。
- 根因：把“落实纲要条目”误当成“完成教材章节”，又把“不主动启动正式审稿”误解为初稿可以只保留骨架；写作前没有逐项列出证明义务和读者需要补足的推理。
- 处理方案：按中心构造和主定理重新列证明义务，从定义、良定义、关键引理、完整证明推进到例子、反例和分层习题；篇幅服从数学任务，不按小节平均分配；交叉复核数学链，并另做语言节奏检查。
- 修改文件：第一卷第 1--3 章正文、`写作规范.md`、`工作流程说明.md`、`审核进度.md`。
- 验证方式与结果：重写后第 1--3 章分别为 22、24、20 个成品页，中文字符量为 9713、8870、8384；完整证明分别为 26、17、25 个，证明概要均为零，习题及解答分别为 8、10、9 组。标签与引用静态检查通过，第一卷双轮编译为 177 页，并完成代表页渲染抽查。
- 后续注意事项：新章开写前先建立证明义务表；软性页数或字符量异常、证明概要过多、习题与正文重合时，不进入“已写完未审核”状态。

### 14.2 单文件 Section 目录层级冗余

- 日期：2026-09-04
- 状态：已解决
- 类型：结构 / 编译
- 影响范围：三卷 344 个节文件、80 个章聚合文件及各节独立编译根指令
- 首个有效错误或现象：每个 `SectionNNNN/` 目录只包含同名的一个 `SectionNNNN.tex`，目录层级没有承载资源分组功能，反而增加路径长度和批量维护成本。
- 根因：早期文件生成规则为潜在的节级附属资源预留了一层目录，但实际项目始终采用单文件节结构。
- 处理方案：先核对 344 个目录均只含一个同名 `.tex` 且目标路径无冲突，再把节文件移到所属 `ChapterNN/` 目录；同步更新 80 个章聚合文件中的 `\input` 路径和 344 个节文件的独立编译根指令，删除空目录。
- 修改文件：三卷全部 `ChapterNN/SectionNNNN.tex`、80 个 `ChapterNN.tex`、`格式规范.md`、`工作流程说明.md`。
- 验证方式与结果：确认旧 `SectionNNNN/SectionNNNN` 引用、节目录和错误根指令均为零；344 个节文件均被唯一引用；三卷分别完成两轮编译。
- 后续注意事项：新建节文件时直接写入所属章目录；批量路径迁移必须同时检查源文件根指令、聚合输入链、规范文档记录和三卷编译，生成脚本不得恢复单文件目录层级。

### 14.3 习题计数与术语显示不一致

- 日期：2026-09-04
- 状态：已解决
- 类型：结构 / 版式 / 索引
- 影响范围：三卷习题编号、正文术语显示及中外文索引
- 首个有效错误或现象：习题与定义、定理等共用计数器，因而不能形成“章号.本章习题序号”的独立序列；术语使用斜体或楷体效果，不够醒目；简单的“人名＋定理”名称在正文重复标出英文，妨碍中文行文。
- 根因：`exercise` 环境复用了定理计数器；`\term` 的正文样式和原文显示策略没有区分普通术语、简单人名定理与具有特殊语义的定理名称。
- 处理方案：把习题改为按章重置的独立计数器，显示为 `章号.序号`；中文术语统一用粗体正体；`\term*` 用于简单“人名＋定理”等正文无需解释原名的名称，只隐藏正文原文而继续登记中外文索引；具有翻译辨识意义的名称仍用普通 `\term` 在正文标出原文。
- 修改文件：`Shared/Preamble.tex`、第一卷第二章相关正文、`格式规范.md`、`术语规范.md`、`工作流程说明.md`。
- 验证方式与结果：三卷各完成两轮 XeLaTeX 编译；第一卷辅助标签确认前三章习题分别从 `1.1`、`2.1`、`3.1` 开始且不占用定理序号；外文索引仍收录 `Carathéodory extension theorem` 与 `Hahn--Kolmogorov theorem`；代表页渲染确认术语为粗体、两处简单人名定理的正文英文已隐藏，页面无新增溢出。
- 后续注意事项：新增习题标签使用 `bN:ex:...`；不要用加粗命令绕过术语宏；是否显示定理原文依据“原名是否补充中文译名的辨识信息”判断，而不是一律显示或一律隐藏。

### 14.4 索引只有空标题页

- 日期：2026-09-04
- 状态：已解决（第一卷）；第二、三卷随正文撰写接入
- 类型：索引 / 编译 / 结构
- 影响范围：第一卷中文索引、外文索引与符号索引；三卷后续正式构建流程
- 首个有效错误或现象：PDF 中三类索引只有标题，没有条目；第一卷构建目录里的 `chinese.idx` 与 `foreign.idx` 已有记录，却不存在相应的 `.ind` 文件。
- 根因：公共导言区使用 `imakeidx` 的 `noautomatic` 选项，因而 XeLaTeX 不会自动调用索引处理器；原编译说明又只有两轮 XeLaTeX。第一卷三个索引入口仍是手写的空章标题和 `TODO`，全项目没有实际读取 `.ind` 的 `\printindex`。
- 处理方案：第一卷三个索引入口分别改用 `\printindex[chinese]`、`\printindex[foreign]` 与 `\printindex[symbols]`；构建流程明确加入三类 `makeindex`，并在其后运行两轮 XeLaTeX。第二、三卷尚无正文索引条目，保留占位入口，待开始撰写时按同一方式接入。
- 修改文件：第一卷三个索引入口、`README.md`、`工作流程说明.md`
- 验证方式与结果：第一卷按“XeLaTeX—三类 `makeindex`—双轮 XeLaTeX”的完整链编译为 207 页；中文、外文和符号索引分别收录 78、78、7 条，三次索引处理均为零拒收、零警告。目录与顶层 PDF 书签均指向逻辑页 e、g、i（物理页 203、205、207），索引链接数分别为 78、78、7，与条目数相符；逐条比对 `.idx`、`.ind` 和 PDF 链接目标，163 条均无错链，抽查第四章和三类索引代表页也未见乱码、裁切或重叠。
- 后续注意事项：新增、删除或移动 `\term`、`\termidx`、`\foreignidx`、`\symidx` 后必须重跑对应索引处理器；旧 `.ind` 不能视为已随正文自动更新。

### 14.5 书目采集与正文写作脱节

- 日期：2026-09-04
- 状态：已解决（第一卷前四章）；后续章节按新流程执行
- 类型：引用 / 内容 / 编译
- 影响范围：第一卷序言、后记、第一编导读、第 1--4 章，以及三卷公共书目工具链
- 首个有效错误或现象：`Shared/References.bib` 已收录 110 条书目，但第一卷正文没有引文，书后的参考文献入口仍是占位内容；已经采集的资料没有进入阅读导航、局部出处说明或成品书目。
- 根因：把“正文应当自足”误解成“不必向读者交代延伸读物与证明来源”，同时逐章流程缺少“来源包—写作接入—书目编译”的闭环，原构建说明也没有运行 Biber。
- 处理方案：接入 `biblatex` 与 Biber；依照“序言负责引导、后记负责回望”的分工重写两处书前书后文字；补写第一编导读，在第 1--4 章章首和八处定理簇连接段加入有明确用途的伴读说明；在写作规范与工作流程中增加逐章来源表、语义位置检查和引文键核对。
- 修改文件：`Shared/Preamble.tex`、`Book1/FrontMatter/Preface/Preface.tex`、`Book1/Part01/Part01.tex`、第一卷第 1--4 章相关源文件、`Book1/BackMatter/Afterword/Afterword.tex`、`Book1/BackMatter/References/References.tex`、`README.md`、`写作规范.md`、`格式规范.md`、`工作流程说明.md`。
- 验证方式与结果：第一卷实际使用 12 个不同的正式书目键，零缺失键，背景资料没有承担正式论证；按“XeLaTeX—Biber—三类 `makeindex`—双轮 XeLaTeX”完整编译为 207 页，正文引文、单页参考文献、目录和 PDF 顶级书签均正常。公共导言区变更后，第二、三卷也分别重编为 115 页和 121 页。
- 后续注意事项：每章开写前先建立来源矩阵；推荐语必须说明资料用途，不能用密集引文代替自足证明；新增、删除或改动引文后须重跑 Biber，且书后只排出本卷实际提及的正式文献。

### 14.6 排版修复与构建说明未同步

- 日期：2026-09-05
- 状态：已解决
- 类型：编译 / 引用 / 书签 / 版式 / 索引
- 影响范围：三卷公共样式、交叉引用边界、目录与书签、第一卷三类索引以及构建说明。
- 首个有效错误或现象：索引右对齐已加入样式，但 README 未传入该样式，样式注释又示范一次合并多个索引；多页码分隔符与规范不一致。交叉引用空格规则未覆盖全部宏写法，规范自身示例仍含字面空格；目录配色、附录独立锚点和习题节点去重的经验未完整收入文档。
- 根因：修复集中于当前源码与 PDF，未同时核对可复现命令、示例、共享样式和规范条款；仅凭字符串扫描或 PDF 抽取文本也不足以判断所有实际空白。
- 处理方案：统一三次独立索引调用及绝对样式路径，显式设置多页码分隔符；修正规范示例并补充宏参数和跨行边界检查；将目录配色、附录锚点、习题唯一书签及结构化验收方法写入对应规范。
- 修改文件：`README.md`、`Shared/analysis.ist`、`格式规范.md`、`写作规范.md`、`工作流程说明.md`；正文及公共宏修复另以本轮源码差异为准。
- 验证方式与结果：三卷完成所需书目、索引及多轮 XeLaTeX 构建，分别为 209、115、121 页。第一卷已知空白点由 PDF 字形坐标确认消除，163 个索引页码链接右对齐且目标正确；奇偶页眉间距同为 10.5 pt，目录分层颜色不变。三卷附录均为唯一顶层节点并有独立目录锚点，80 个正文章各有一个习题书签。另在 `tmp/` 用同一测试条目的页码 1、3 单独运行索引处理器，所得 `.ind` 含右对齐命令与全角逗号加空格；测试条目未进入正文。未追加页面渲染。Windows 下 Biber 使用 README 所示的 ASCII 相对构建路径，避免原生工具无法打开含中文的绝对路径；索引样式的绝对路径已经实际构建验证。
- 后续注意事项：构建命令与规范一同维护；本轮结果在 `审核进度.md` 的横向检查表登记，不增加逐章数学审核轮数。只修改文档时先核对示例；涉及共享宏或索引样式时按第 5、6、7 节完成对应构建和结构化检查。
