# 《分析学》LaTeX 与排版规范

版本：0.1（2026-09-02）

本规范先固定工程骨架和基础视觉语言，不在项目初期锁死所有字号、间距和书目细节。后续以实际章节的编译效果逐步补充。

## 1. 分卷与主文件

三卷分别独立编译、独立生成 PDF：

- `Book1/Book1.tex`
- `Book2/Book2.tex`
- `Book3/Book3.tex`

根目录不设合订本 `Main.tex`。各卷主文件只负责文档类、卷元数据、公共导言区以及书前—正文—附录—书后的装配，不在主文件中写正文。

三卷共用 `Shared/Preamble.tex`，以保证宏、字体、定理环境和基础版式一致；书名页、序言、后记、参考文献和索引则分别保存在对应的 `BookN` 目录中。

## 2. 目录与文件命名

正文层级统一为：

```text
BookN/PartNN/ChapterNN/SectionCCSS/SectionCCSS.tex
```

- `BookN` 不补零；`PartNN`、`ChapterNN` 使用两位数字；
- `SectionCCSS` 的前两位为章号，后两位为节号；
- 目录中的聚合文件与目录同名；
- 附录使用 `Appendices/AppendixA/AppendixA.tex`，每卷从 A 重新开始；
- 正文目前拆分到 `section` 为止，不机械增加更细的文件层级。

所有 `\input` 使用相对于本卷主目录的路径。编辑器根文件注释应指向所在卷的 `BookN.tex`。

## 3. 基础版式

当前版式参考现代中文数学教材，并吸收《集合论、数理逻辑与计算机》项目中较克制的蓝灰视觉风格：

- XeLaTeX、`ctexbook`、小四号正文；
- A4 页型，双面页眉，章首页只保留页脚页码；
- 中文使用可移植的 Fandol 字体，西文与数学使用 TeX Gyre Pagella；
- 主色为深蓝，辅助色为中性灰，避免高饱和装饰；
- 段首缩进 `2em`，段间距为零，行距暂定约 `1.18`；
- 编、章、节形成清楚的字号层级，定理环境不使用厚重彩框。

`openany`、页边距、字体和颜色均视为当前基线；在形成若干完整章节后，可根据印刷和屏幕阅读效果统一调整。

## 4. 数学排版

- 行间公式使用标准 LaTeX 环境，不使用 `$$ ... $$`；
- 多行推导按语义选用 `align`、`gather`、`multline` 等环境；
- 定义、定理、命题、引理和推论共享统一编号体系；
- 公式、结果、章节和图表均通过 `\label` 与 `\ref`/`\cref`/`\eqref` 引用，不手写编号；
- 矩阵、分段函数、交换图及长公式首先保证数学结构清楚，其次再处理紧凑性；
- 中文句子中的公式仍服从中文标点和完整句法。

## 5. 书前与书后

每卷均预留：书名页、序言、目录、后记、参考文献、中文索引、外文索引和符号索引。三类索引使用不同命名空间，正文写作时通过语义宏逐步积累，不在完稿后手工拼接。

参考文献与索引的最终 Biber/MakeIndex 流程待实际条目出现后统一确定；当前文件不得删除或合并。

## 6. 编译与验收

编译时进入对应卷目录，以该卷主文件为入口。阶段性提交至少满足：

- XeLaTeX 无错误；
- 所有 `\input`、标签和交叉引用可解析；
- 无未处理的 citation；
- 无明显 overfull box；
- 标题页、目录、章首页、长公式、表格、参考文献和索引页经必要的局部检查。

日志检查优先于逐页截图；只有日志不足以判断的版面问题才进行页面渲染。
