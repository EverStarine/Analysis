# 《分析学》LaTeX 与排版规范

版本：1.9（2026-09-06）

本规范统一三卷的工程结构、编号、交叉引用和页面版式。术语选择见 `术语规范.md`，论述与习题写法见 `写作规范.md`，实际操作顺序见 `工作流程说明.md`。

## 1. 分卷、公共文件与编译入口

三卷分别以 `Book1/Book1.tex`、`Book2/Book2.tex`、`Book3/Book3.tex` 为唯一主文件，独立生成 PDF；根目录不设合订本 `Main.tex`。

- 主文件只负责卷元数据、公共导言区以及书前—正文—附录—书后的装配。
- 共用宏和版式统一放在 `Shared/Preamble.tex`。
- 全书只使用一个书目数据库 `Shared/References.bib`；各卷只打印本卷实际引用的条目。
- 正文层级拆到 `section` 为止，不机械建立更细的文件目录。

## 2. 路径、命名与标签

正文路径统一为：

```text
BookN/PartNN/ChapterNN/SectionCCSS.tex
```

`BookN` 不补零；`PartNN`、`ChapterNN` 使用两位数字；`SectionCCSS` 的前两位是章号，后两位是节号。编与章的聚合文件和所在目录同名；节文件直接放在所属 `ChapterNN/` 中，不为单个 `SectionCCSS.tex` 再建立同名目录。附录的物理路径继续采用 `Appendices/AppendixA/AppendixA.tex`，不因显示编号改变而重命名。

编、章、节、小节以及除证明类环境以外的所有定理环境，默认紧跟一个 `\label`。建议使用以下稳定前缀：

```text
bN:part:NN       编
bN:ch:NN         章
bN:sec:CCSS      节
bN:subsec:...    小节
bN:app:A         附录物理标识
bN:thm:...       定理类环境
bN:ex:...        习题
bN:eq:...        被引用的公式
bN:item:...      列表项
```

标签是稳定标识，不把可能变化的显示标题写进标签。小节标签按“节号＋节内流水（两位补零）”编码，如某节第 3 个小节为 `b1:subsec:010103`；小节骨架重排时流水按纲要顺序重新编号。跨册引用另按 `跨卷引用表.md` 登记。

## 3. 页面、页码与页眉

- 书名页的标题层级固定为三行：“分析学”“卷次”“卷副标题”。卷次必须紧接总书名，不能放在副标题之后；副标题使用三卷统一的字号，并以最长的第一卷标题仍能单行排下为下限。
- 封面必须有顶级 PDF 书签。书签名称由卷级书前入口显式指定，可使用具有辨识性的卷副标题，不强制写成笼统的“封面”；第一卷使用“实分析：测度、几何测度与 Sobolev 空间”。
- 书前使用小写罗马数字页码；正文与附录共用一套阿拉伯数字页码；书后重新使用小写拉丁字母页码。
- 书名页后保留一张空白页（不显示页码，计入页序），序言自第 iii 页开始。
- 序言、目录，以及书后的后记、参考文献、中文索引、外文索引、符号索引，各自从奇数页开始；前一元素落在偶数页时以空白页（计页序、不显示页码）补齐，使后续元素自然从奇数页开始。
- 普通正文页的页码放在页眉：偶数页为“页码＋章序与章标题”，章序与章标题之间以 `\quad` 分隔，间距与奇数页“节号＋节标题”之间的间距一致（章序随章类显示，如“第一章 集合系、可测空间与可测映射”，附录章为“A1 …”）；奇数页为“节号＋节标题＋页码”。页眉保留细横线。
- 无编号的章级单元（序言、目录、编导读、后记、参考文献、索引等）自其第二页起，页眉同样显示该单元自身标题；该类单元首页与其他章首页相同，不设页眉及横线，页码改放页脚中央。
- 编首页和章首页不设页眉及横线，页码改放页脚中央。
- 每一编必须从奇数页开始；编题页之后保留一张真正的空白页，再进入本编正文。
- 空白页不显示页眉、页脚或页码，但仍计入物理页序。

这里的“普通页页码在页眉、编章首页页码在页脚”是统一规则；不得在各章临时另设页式。

## 4. 标题、选读标记与附录

正文中的选读标记 `*` 属于章节序号，不属于标题文字。显示形式应为“第十二章* 标题”或“3.5* 标题”，不得写成“标题*”。源码使用项目提供的选读标题命令，禁止直接把星号塞进标题参数。小节选读同样使用共享宏 `\OptionalSubsection{标题}{标签}`：星号随小节编号显示（如“9.4.4*”），标题参数不得含星号。弱-* 拓扑等数学术语中的星号不属于选读标记。

纸面目录按层级统一配色：编级条目为黑色，章级和节级条目为 `AnalysisBlue`，小节级条目为 `AnalysisGray`；同一条目的标题与页码使用相应层级颜色。配色由公共样式控制，各卷不另设规则。

附录天然是选读材料，无论难度如何都不再添加 `*`。各卷的附录显示编号为：

- 第一卷：A1、B1、C1……；
- 第二卷：A2、B2、C2……；
- 第三卷：A3、B3、C3……。

每卷仍只有一个无编号“附录”编，单个附录按章级排版。附录不分节时，定理编号形如 `A1.1`；分节时形如 `A1.2.1`。物理目录名仍为 `AppendixA`、`AppendixB`，避免无必要的大规模改名。

“附录”在 PDF 书签树中必须是唯一的顶级节点，直接挂接本卷各附录章，不得位于最后一个正文编内。统一用 `\BookAppendixPart` 建立此节点；目录中的“附录”行须有独立锚点，公共宏内先设置 `startatroot`，再以 `\phantomsection` 建立目标，随后执行 `\addcontentsline`，不得沿用前一节的目标或另加重复的手写书签。

## 5. 定理与证明环境

具名结果用环境的可选参数标出名称，例如：

~~~tex
\begin{theorem}[Tonelli]\label{b1:thm:tonelli}
具体陈述。
\end{theorem}
~~~

成品标题由环境自动排出“定理 7.3.1（Tonelli）”这一结构，不手写定理编号。Hahn 分解、单调收敛等保留必要的名称限定；同一定理的不同版本在可选参数中简明标明。环境类别已出现时一般不再在括号中重复“定理”或英文 theorem。名称仅作标题文本，不在可选参数中调用术语或索引宏，以免重复入索引；首次引入处原有的中外文索引保留。只补名称时不改环境类别、计数器和稳定标签。

以下有编号环境共享同一计数器，统一按“章.节.本节流水号”编号：

```text
definition   定义
proposition  命题
lemma        引理
theorem      定理
claim        断言
remark       注记
corollary    推论
conjecture   猜想
openproblem  开放问题
```

附录的无节情形按上一节所述省略中间的 `0`。上述环境虽然共享流水号，但交叉引用类型必须彼此独立：`\cref` 应分别显示“定义”“命题”“引理”“定理”等实际环境名称，不得因共享计数器统一退化成“定理”。除证明类环境外，上述环境均默认设置标签。若确需增加新的定理环境，必须先与编写者商议，不得自行扩充。

`exercise` 使用独立计数器，不占用、也不续接上述定理类流水号；每章从 1 重新开始，统一显示为“章号.章内习题号”，例如第 3 章依次为“习题 3.1”“习题 3.2”。习题即使写在某一节中也不加入节号。附录沿用附录章号，例如第一卷附录 A1 中依次显示为“习题 A1.1”“习题 A1.2”。

章末无编号习题标题使用共享命令 `\BookExercises`，由该命令生成“习题”标题、更新奇数页页眉并写入节级目录项。每章只能有一个“习题”书签，挂在所属章下；书签由命令内部的章节与目录机制生成，调用处不得再叠加 `\pdfbookmark` 或重复的 `\addcontentsline`。不得只写 `\section*{习题}` 而让页眉继续显示上一正文节。

`proof`、`solution`、`proofsketch` 分别显示“证明”“解”“证明概要”，均不编号。证明终止符由环境自动产生，不手写“证毕”。

## 6. 列表与列表项引用

普通 `enumerate` 的默认序号为：

1. 第一层：`1.`、`2.`、`3.`；
2. 第二层：`1)`、`2)`、`3)`；
3. 第三层：`a)`、`b)`、`c)`。

尽量不嵌套到第四层；确有必要时，第四层改用 `itemize`。表示“下列命题等价”时使用专用等价列表：第一层为 `I.`、`II.`、`III.`、`IV.`，第二层为 `i)`、`ii)`、`iii)`、`iv)`。所有序号都用正体。

每个列表项都必须在 `\item` 后立即设置 `\label`。第二项的引用必须补齐左括号，使用 `\itemref{...}` 一类统一命令输出 `(2)`，不得只写 `2)`。例如：

```tex
\cref{b1:thm:sample}的\itemref{b1:item:sample-2}
```

## 7. 交叉引用与超链接范围

- 能自然使用 `\cref` 时优先使用；需要控制中文句法或链接范围时使用 `\ref`。
- 不手写会随增删而变化的章、节、定理、公式或图表编号。
- 同类结果并列写成“定理 x 与 y”时，可用 `\cref{thm:x}与\ref{thm:y}`，使第一处链接覆盖“定理 x”，第二处只链接 `y`；不要机械重复成“定理 x 与定理 y”。
- 提及前文或后文的章、节和结果都应设置超链接，反向引用与前向引用均允许。
- 交叉引用与超链接命令（`\ref`、`\cref`、`\eqref`、`\itemref`、`\term`、`\hyperref` 及适用的星号形式，如 `\ref*`）与相邻中文或全角标点之间一律不留多余空格。同行字面空格或显式不换行空格 `~` 均须清理；源码换行造成的空格，应先删除行末已有空白，再紧接 `%` 抑制。既要检查宏前后边界，也要检查 `\hyperref` 显示参数内中文与编号的连接，例如 `\hyperref[b1:thm:sample]{定理\ref*{b1:thm:sample}}`。
- 此类空格须结合源码边界与 PDF 文本双向核对，不能只搜索宏名单或将换行摊平成普通字符串。PDF 文本抽取会自行插入或省略空格；遇到疑点，应核对相邻字形及链接区域的坐标，区分实际排版间距与抽取结果，不据抽取字符串单独下结论。

## 8. 段落与数学公式

- 两个连续的正文段落在 `.tex` 源码中必须用一个空行分隔；不要靠强制换行伪造分段。
- 保持全局 `\parskip` 为零，不为正文、列表或定理环境局部乱改行距和段距。
- 行内公式统一使用 `\(...\)`，不用 `$...$`；行间公式使用 `\[...\]` 或标准数学环境，绝不使用 `$$...$$`。这一限制只针对 `.tex` 源码，不要求机械改写 Markdown 数学块。
- 行间公式仅在后文确有引用时使用带编号环境并设置 `\label`；否则不编号。
- 括号原则上使用 `\left`、`\right` 自动配对。同类可见括号嵌套时，外层须比内层稍大一级；即使自动尺寸相同，也应改用成对的 `\bigl/\bigr`、`\Bigl/\Bigr` 等明确区分，例如 `\bigl(f(x)\bigr)`、`\Bigl(\bigl(f(x)\bigr)^2\Bigr)`。圆括号、方括号、可见花括号及同类绝对值／范数界符均按此处理；不改变区间开闭性，不把源码分组花括号当成可见括号，也不为追求大小而无节制放大。行内分数优先使用 `\dfrac`；若分式过高破坏行距，应改为行间公式或酌情使用较小样式。
- 两个绝对值或范数因子相乘时，必须在其间写 `\cdot`，包括带上下标、幂以及两类界符混合的情形。例如 `|a|\cdot|b|`、`\|f\|_p\cdot\|g\|_q`、`|a|\cdot\|x\|`。本规则针对乘积，不把集合分隔线、条件符号或嵌套界符误改成乘法。
- 所有数学模式统一使用 `unicode-math` 的默认数学字体 Latin Modern Math，包括行内 `\(...\)`、行间 `\[...\]`、各标准公式环境，以及标题、页眉、目录和索引中的数学内容。其中 `\mathbb{R}` 等黑板粗体字母按编写者指定统一采用传统 AMS 字体（MSBM），行内、行间一致。非数学西文字母的试排规则不适用于公式；不得按行内、行间另设不同数学字体，也不另指定 Pagella Math 或建立局部数学版本。
- 2026-09-06 按编写者确认进行字母级试排：非数学模式中的西文字母统一使用与定理正文同款的 Pagella 斜体，普通字重用 Pagella Italic，原有粗体字母用 Pagella Bold Italic，保留粗细区别。括号、标点、数字保持原有字体与字形设置，不随此次处理改变；中文字体和字形不变。此项由共享设置统一实现，不能用整段 `\itshape`，也不能把 `\setmainfont` 的正体直接映射为 Italic，以免连带改变非字母字符或中文。当前为试排供验收，不记作最终版式确认；操作记录见工作流程说明 2.4 第 14.0 节。
- 术语后的原文注记统一用半角圆括号 `(原文)`，由共享 `\term` 宏生成；中文主称的中文字形仍为粗体正体，原文中的西文字母按上述试排使用 Pagella 斜体且不加粗，外侧半角括号仍为正体。原文中的数字及其他标点不随字母倾斜，三类索引的行为不变。普通中文解释性括注和定理标题括号不因这条规则一并替换；术语显示细则见术语规范 1.7。
- 行内映射箭头使用 `\rightarrow`、`\mapsto`；行间映射箭头使用 `\longrightarrow`、`\longmapsto`。
- 交换图统一使用 `tikzcd`。行间公式中的普通分隔使用 `\quad`，不用 `\qquad`。
- 集合构造式在花括号内侧及竖线或冒号两侧留适度数学空隙，例如 `\{\,x\in X \mid P(x)\,\}`。
- 公式中能用数学符号表达时，不嵌入“使得、且、或、所有、存在”等中文；必须出现中文时使用正体文字命令。

## 9. 图、表与算法

图、表和算法一旦出现，必须分别编号、设置标签并给出标题。某卷首次使用其中一种浮动对象后，应在总目录之后加入对应的图目录、表目录或算法目录；没有该类对象时不生成空目录。

算法必须明确写出输入、输出和步骤。插入任何图、表、算法或交换图后，都要检查浮动位置、缩放、跨页和正文阅读顺序，不能只确认编译成功。是否值得使用图表由 `写作规范.md` 决定。

## 10. 书目、索引与书后

三卷通过公共导言区统一使用 `biblatex + biber`，书目数据只取自 `Shared/References.bib`。正文用 `\cite{key}` 建立链接，参考文献入口用 `\printbibliography` 排出本卷实际引用的条目；正式成品不得用 `\nocite{*}` 把整个共享数据库灌入单卷。作者、书名或文章名的正文写法及资料资格见 `写作规范.md`，背景材料与例外候选的状态以书目数据库和来源台账为准。

第一次 XeLaTeX 后必须运行 Biber，再运行三类索引处理器（以 `Shared/analysis.ist` 为样式）和两轮 XeLaTeX。索引处理器须对 `chinese`、`foreign`、`symbols` 三个索引名分别单独运行，以 `-s` 传入样式的绝对路径；每次只处理一个 `.idx`，多文件一次调用会按合并语义写入首个 `.ind`，不得使用。可直接执行的分卷构建命令见 `README.md`。书目发生增删、citation key 改动或引用位置变化时，不得沿用旧 `.bbl` 判断成品已经更新；正文页码变动后同样不得沿用旧 `.ind`。

三类索引分别维护：

- 中文索引按汉语拼音首字母 a—z 排列；
- 外文索引按原文 a—z 字典序排列；
- 符号索引按符号首次出现次序排列；
- 每个词条的页码链接右对齐：页码列贴行末，同一词条的多页码以“， ”分隔；右对齐样式统一由 `Shared/analysis.ist` 提供，索引处理器须以 `-s` 指定该文件。

一个符号占一个主条目，条目链接到首次定义或约定的位置。索引技术细节和收词范围见 `术语规范.md`。后记、参考文献和三类索引必须作为书后顶级书签，不得挂在最后一个附录下面。

索引参数同时受 TeX 和 `makeindex` 两层语法约束。例如范数在 `\symidx` 中写成 `\lVert f\rVert_1`，不用含裸竖线的 `\|f\|_1`，避免 `|` 被解释为页码处理分隔符。索引程序退出成功并不保证词条全部收录；构建后逐类检查 `.ilg` 的拒收数和警告数均为零，再比对 `.idx`、`.ind` 与 PDF 中的实际词条。

## 11. 编译验收

阶段性验收至少满足：XeLaTeX 无错误；输入链完整；标签、书签和引用可解析；无明显溢出版心；页码分区、奇偶页眉、编与附录的起始页正确。日志判断优先，图像检查按 `工作流程说明.md` 的节制原则执行。
